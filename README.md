# Searching Algorithms

A collection of searching algorithms, with implementations and examples.

## Algorithms

- **Linear Search** — checks each element in sequence.
- **Binary Search** — searches a sorted collection by repeatedly halving the search range.

## Requirements

List the language version and any dependencies needed to run the code.

## Usage

Explain how to run an algorithm and show a small example. For instance:

```text
Input:  [2, 5, 8, 12, 16]
Target: 12
Output: Index 3

  time complexity ---
| Algorithm     | Best | Average  | Worst    | Requirement          |
| Linear Search | O(1) | O(n)     | O(n)     | None                 |
| Binary Search | O(1) | O(log n) | O(log n) | Input must be sorted |
